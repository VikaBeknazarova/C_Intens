#include <iostream>
#include <cmath>

using namespace std;

int main() {
    double a, y;
    cout << "Введите значение a: ";
    cin >> a;
    y = pow(a, 2) + 1 / sqrt(pow(a, 2) + 1);
    cout << "Значение функции y = " << y << endl;
    return 0;
}
